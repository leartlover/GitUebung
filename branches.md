```bash
# Git Branches - Was sind das?
echo "Branches in Git sind parallele Versionen eines Projekts. Sie erlauben es Entwicklern, unabhängig an verschiedenen Features oder Fixes zu arbeiten, ohne den Hauptcode zu beeinflussen."

# Erstellen eines neuen Branches
echo "Befehl: git branch <branch-name>"

# Wechseln zu einem anderen Branch
echo "Befehl: git checkout <branch-name>"

# Zusammenführen eines Branches in den Hauptbranch
echo "Befehl: git merge <branch-name>"

# Löschen eines Branches
echo "Befehl: git branch -d <branch-name>"
    